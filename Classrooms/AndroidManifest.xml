<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.classrooms"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="20" />
    
    <supports-screens
			android:smallScreens="true"
				android:normalScreens="true"
				android:largeScreens= "true"
				android:anyDensity="true"
		/>
    

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.login"
            android:label="Classrooms" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.register"
            android:label="Register" >
            <intent-filter>

            </intent-filter>
        </activity>
        <activity
            android:name=".activity.home"
            android:label="Classrooms" >
            <intent-filter>

            </intent-filter>
        </activity>
        <activity
            android:name=".activity.profile"
            android:label="Profile" >
            <intent-filter>

            </intent-filter>
        </activity>
        <activity
            android:name=".activity.homeExploded"
            android:label="Profile" >
            <intent-filter>

            </intent-filter>
        </activity>
        <activity
            android:name=".activity.classroomActivity"
            android:label="" >
            <intent-filter>

            </intent-filter>
        </activity>
        <activity
            android:name=".activity.makePost"
            android:label="Write Post" >
            <intent-filter>

            </intent-filter>
        </activity>
        <activity
            android:name=".activity.makeClassroom"
            android:label="Classroom Creation" >
            <intent-filter>

            </intent-filter>
        </activity>
        <activity
            android:name=".activity.postActivity"
            android:label="" >
            <intent-filter>

            </intent-filter>
        </activity>
    </application>

</manifest>
